name: setting_data
description: "A new Flutter package project."
version: 0.0.1

publish_to: 'none'

environment:
  sdk: ^3.8.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  preferences:
    path: ../../core/preferences

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any
